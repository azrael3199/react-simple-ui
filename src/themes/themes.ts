import { IThemeProperties } from '../types/common.types';

export const defaultTheme: IThemeProperties = {
  primaryColor: 'black',
  accentColor: 'white',
  bgColor: '#AAAAAA',
  fonts: {
    fontFamily: 'https://fonts.googleapis.com/css?family=Roboto',
  },
};
